//PEGANDO ELEMENYOS DO DOM
   const formando = document.queryselector('informulario-pessoa')
   const divResultado = document.querySelector('iv-dados>')

   //CAPTURANDO O EVENTO SUBMIT DO FORMULÁRIO
   formDADOS.addEventlistener('submit', (evt)=>)